# Local application state. Keep this outside every .photoslibrary bundle.
APPLE_PHOTOS_STATE_DIR=./state

# Optional path to a locally built PhotoKitHelper executable.
APPLE_PHOTOS_HELPER=/usr/local/bin/PhotoKitHelper

# Live tests are destructive and remain disabled by default.
APPLE_PHOTOS_ENABLE_LIVE_TESTS=0
